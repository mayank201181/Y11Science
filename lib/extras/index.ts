import type { TopicExtras } from "../types";

// Extras modules are imported here as they are authored by the extras pass.
// Each file exports `export const extras: TopicExtras`.

export const EXTRAS_LIST: TopicExtras[] = [];
